---
phase: 02-game-rules-and-local-play
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/hex/types.ts
  - src/lib/game/rules.ts
  - src/lib/game/rules.test.ts
autonomous: true
requirements: [GAME-01, GAME-02, GAME-03, GAME-04, GAME-05, GAME-08, GAME-09, UI-03]

must_haves:
  truths:
    - "X places exactly 1 stone on the first turn"
    - "After the first turn, each player places exactly 2 stones per turn"
    - "6 consecutive stones along any of the 3 hex axes is detected as a win"
    - "Win detection checks q-axis, r-axis, and s-axis directions"
    - "Game ends immediately when a win is detected mid-turn"
    - "Rematch resets board and swaps starting player to the loser"
    - "Move counter increments on each stone placement"
  artifacts:
    - path: "src/lib/hex/types.ts"
      provides: "Player, GameStatus, GameSnapshot types"
      contains: "export type Player"
    - path: "src/lib/game/rules.ts"
      provides: "Pure game logic functions"
      exports: ["coordKey", "checkWinFromHex", "isValidMove", "createInitialSnapshot", "applyMove", "applyRematch"]
    - path: "src/lib/game/rules.test.ts"
      provides: "Unit tests for all game rules"
      min_lines: 100
  key_links:
    - from: "src/lib/game/rules.ts"
      to: "src/lib/hex/types.ts"
      via: "imports HexCoord, Player, GameSnapshot"
      pattern: "import.*from.*hex/types"
    - from: "src/lib/game/rules.ts"
      to: "src/lib/hex/math.ts"
      via: "imports HEX_DIRECTIONS for axis traversal"
      pattern: "import.*HEX_DIRECTIONS.*from.*hex/math"
---

<objective>
Build the pure game logic for hex Connect6 with full TDD coverage.

Purpose: Establish a fully-tested, pure-function rules engine that subsequent plans wire into reactive state and canvas rendering. All game logic is testable without Svelte, DOM, or canvas dependencies.

Output: `src/lib/game/rules.ts` with pure functions, `src/lib/game/rules.test.ts` with comprehensive tests, extended types in `src/lib/hex/types.ts`.
</objective>

<execution_context>
@/home/haoye/Source/hex-tic-tac-toe/.claude/get-shit-done/workflows/execute-plan.md
@/home/haoye/Source/hex-tic-tac-toe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-game-rules-and-local-play/02-CONTEXT.md
@.planning/phases/02-game-rules-and-local-play/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From src/lib/hex/types.ts:
```typescript
export interface HexCoord {
  q: number;
  r: number;
}

export interface Point {
  x: number;
  y: number;
}

export interface Camera {
  x: number;
  y: number;
  zoom: number;
}
```

From src/lib/hex/math.ts:
```typescript
export const HEX_DIRECTIONS: HexCoord[] = [
  { q: 1, r: 0 }, { q: 1, r: -1 }, { q: 0, r: -1 },
  { q: -1, r: 0 }, { q: -1, r: 1 }, { q: 0, r: 1 },
];
// Axis pairs for win detection: [0,3], [1,4], [2,5]
// Axis 0: q-axis (East/West)
// Axis 1: r-axis (NE/SW)
// Axis 2: s-axis (NW/SE)

export function hexRound(q: number, r: number): HexCoord;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define game types and implement pure rules with TDD</name>
  <files>src/lib/hex/types.ts, src/lib/game/rules.ts, src/lib/game/rules.test.ts</files>
  <read_first>
    - src/lib/hex/types.ts (current types to extend)
    - src/lib/hex/math.ts (HEX_DIRECTIONS for axis pairs, hexRound for coord normalization)
    - src/lib/hex/math.test.ts (test style conventions)
    - .planning/phases/02-game-rules-and-local-play/02-RESEARCH.md (architecture patterns, code examples)
    - .planning/phases/02-game-rules-and-local-play/02-CONTEXT.md (locked decisions D-01 through D-15)
  </read_first>
  <behavior>
    RED phase -- write failing tests first, then GREEN phase -- implement to pass:

    Types to add to src/lib/hex/types.ts:
    - `export type Player = 'X' | 'O';`
    - `export type GameStatus = 'playing' | 'won';`
    - `export interface GameSnapshot { board: Map&lt;string, Player&gt;; currentPlayer: Player; placementsThisTurn: number; isFirstTurn: boolean; totalMoves: number; status: GameStatus; winner: Player | null; winningLine: HexCoord[]; startingPlayer: Player; }`

    Functions to implement in src/lib/game/rules.ts:
    - `coordKey(hex: HexCoord): string` -- returns `"${hex.q},${hex.r}"`
    - `createInitialSnapshot(startingPlayer?: Player): GameSnapshot` -- returns fresh game state
    - `isValidMove(snapshot: GameSnapshot, hex: HexCoord): boolean` -- checks hex is empty and game is playing
    - `checkWinFromHex(board: Map&lt;string, Player&gt;, hex: HexCoord, player: Player): HexCoord[] | null` -- checks 3 axis pairs using HEX_DIRECTIONS[0,3], [1,4], [2,5]; returns winning coords or null
    - `applyMove(snapshot: GameSnapshot, hex: HexCoord): GameSnapshot` -- places stone, checks win, advances turn; returns NEW snapshot (immutable)
    - `applyRematch(snapshot: GameSnapshot): GameSnapshot` -- resets board, loser goes first per D-10

    Test cases in src/lib/game/rules.test.ts:
    - GAME-01: First turn allows exactly 1 placement for X, then switches to O
    - GAME-02: Second turn allows 2 placements for O, then switches to X with 2 placements
    - GAME-03: 6 consecutive X stones along q-axis returns winning line
    - GAME-04: Win detected on r-axis (NE/SW) and s-axis (NW/SE) separately
    - GAME-05: Win on first of 2 placements ends game immediately (no second placement allowed)
    - GAME-08: applyRematch resets board, status, totalMoves; returns playing state
    - GAME-09: applyRematch after X wins sets startingPlayer and currentPlayer to 'O' (loser goes first per D-10)
    - UI-03: totalMoves increments by 1 on each stone placed
    - Edge: isValidMove returns false for occupied hex
    - Edge: isValidMove returns false when status is 'won'
    - Edge: coordKey produces consistent keys (no -0 issues since hexRound normalizes)
  </behavior>
  <action>
    Step 1 -- Extend types (src/lib/hex/types.ts):
    Add at the bottom of the existing file (do NOT remove existing HexCoord, Point, Camera):
    ```typescript
    export type Player = 'X' | 'O';
    export type GameStatus = 'playing' | 'won';

    export interface GameSnapshot {
      board: Map<string, Player>;
      currentPlayer: Player;
      placementsThisTurn: number;
      isFirstTurn: boolean;
      totalMoves: number;
      status: GameStatus;
      winner: Player | null;
      winningLine: HexCoord[];
      startingPlayer: Player;
    }
    ```

    Step 2 -- RED: Write test file src/lib/game/rules.test.ts with all test cases listed in behavior. Use vitest `describe`/`it` blocks. Import from '../game/rules' (file does not exist yet -- tests must fail).

    Step 3 -- Run `npx vitest run src/lib/game/rules.test.ts` -- confirm all tests fail (RED).

    Step 4 -- GREEN: Create src/lib/game/rules.ts implementing all functions:

    `coordKey`: Return template literal `${hex.q},${hex.r}`.

    `createInitialSnapshot(startingPlayer: Player = 'X')`: Return GameSnapshot with empty Map, startingPlayer as currentPlayer, placementsThisTurn: 0, isFirstTurn: true, totalMoves: 0, status: 'playing', winner: null, winningLine: [].

    `isValidMove(snapshot, hex)`: Return `snapshot.status === 'playing' && !snapshot.board.has(coordKey(hex))`.

    `checkWinFromHex(board, hex, player)`: For each of 3 axis pairs (indices [0,3], [1,4], [2,5] of HEX_DIRECTIONS), count consecutive same-player stones in both directions from hex. If total line length >= 6, return the line coords. Otherwise null.

    `applyMove(snapshot, hex)`:
    1. If `!isValidMove(snapshot, hex)` return snapshot unchanged.
    2. Create new Map from snapshot.board, set coordKey(hex) to currentPlayer.
    3. Increment totalMoves and placementsThisTurn.
    4. Check win via checkWinFromHex. If win found, set status='won', winner=currentPlayer, winningLine=result, return new snapshot.
    5. Compute maxPlacements = isFirstTurn ? 1 : 2. If placementsThisTurn >= maxPlacements, swap player, reset placementsThisTurn to 0, set isFirstTurn=false.
    6. Return new snapshot (spread old, override changed fields, new board Map).

    `applyRematch(snapshot)`:
    1. Determine new starting player: if snapshot.winner exists, the OTHER player (loser) starts. Otherwise swap from snapshot.startingPlayer.
    2. Return createInitialSnapshot(newStartingPlayer).

    Step 5 -- Run `npx vitest run src/lib/game/rules.test.ts` -- all tests pass (GREEN).

    Step 6 -- REFACTOR if needed: extract `countInDirection` helper if checkWinFromHex is over 30 lines.
  </action>
  <verify>
    <automated>npx vitest run src/lib/game/rules.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/hex/types.ts contains `export type Player = 'X' | 'O'`
    - src/lib/hex/types.ts contains `export type GameStatus = 'playing' | 'won'`
    - src/lib/hex/types.ts contains `export interface GameSnapshot`
    - src/lib/game/rules.ts contains `export function coordKey(`
    - src/lib/game/rules.ts contains `export function createInitialSnapshot(`
    - src/lib/game/rules.ts contains `export function isValidMove(`
    - src/lib/game/rules.ts contains `export function checkWinFromHex(`
    - src/lib/game/rules.ts contains `export function applyMove(`
    - src/lib/game/rules.ts contains `export function applyRematch(`
    - src/lib/game/rules.ts contains `import { HEX_DIRECTIONS } from`
    - src/lib/game/rules.test.ts contains at least 10 `it(` or `test(` blocks
    - `npx vitest run src/lib/game/rules.test.ts` exits with code 0
  </acceptance_criteria>
  <done>All game rule functions implemented as pure functions with TDD. Tests cover: first-turn 1 stone, subsequent 2-stone turns, win detection on all 3 axes, immediate game end on mid-turn win, rematch with loser-goes-first, move counter increment. All tests green.</done>
</task>

</tasks>

<verification>
```bash
npx vitest run src/lib/game/rules.test.ts --reporter=verbose
```
All tests pass. No Svelte, DOM, or canvas dependencies in rules.ts.
</verification>

<success_criteria>
- Pure game logic fully tested and passing
- Types exported from hex/types.ts for use by Plan 02 and Plan 03
- Zero external dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-rules-and-local-play/02-01-SUMMARY.md`
</output>
